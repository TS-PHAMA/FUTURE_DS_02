# Install required packages
packages <- c("tidyverse", "lubridate", "janitor", "scales", 
              "ggplot2", "corrplot", "broom", "survival", 
              "survminer", "gridExtra", "knitr")

install.packages(packages)
